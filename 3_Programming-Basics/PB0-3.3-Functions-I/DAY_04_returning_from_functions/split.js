

const str = 'aaaaaaaaaaaaaaaaaaaaaaaa';

const parts = str.split('a');

// console.log(`There are ${str.split('a').length - 1} a's in the string.` + str.length);



const p = 'The quick brown fox jumps over the lazy dog. If the dog reacted, was it really lazy?';

// console.log(p.replace('dog', 'monkey'));

const loggedIn = 'yes';
const status = loggedIn === 'yes' ? 'online' : 'offline';
console.log(status);